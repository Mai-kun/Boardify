<UserControl x:Class="BoardifyApp.Views.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BoardifyApp.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BoardViewModel}"
             Height="Auto" Width="Auto">

    <DockPanel Margin="20">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" VerticalAlignment="Center" Margin="0 0 0 20">
            <Button Content="← Назад"
                    Command="{Binding BackCommand}"
                    Padding="6" Background="#E0E0E0" />
            <TextBlock Text="{Binding Board.Name}" FontSize="24" FontWeight="Bold" Margin="20 0 0 0"
                       VerticalAlignment="Center" />
        </StackPanel>

        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Columns}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#EEE" CornerRadius="5" Margin="10" Padding="10" Width="250">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Column.Title}"
                                           FontWeight="Bold"
                                           FontSize="18"
                                           TextAlignment="Center"
                                           Margin="0 0 0 10" />

                                <ItemsControl ItemsSource="{Binding Tasks}"
                                              Margin="0 10 0 0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#AAAAA"
                                                    BorderBrush="#CCC"
                                                    BorderThickness="1"
                                                    CornerRadius="5"
                                                    Padding="10"
                                                    Margin="0 5">
                                                <TextBlock Text="{Binding TaskCard.Title}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>